# AsrTools 优化清单

## 背景说明
当前项目中，除了B接口（必剪ASR）外，其他接口（J接口-剪映、K接口-快手、Whisper）均已失效或未实现。需要进行以下两项主要优化：

---

## 1. 界面接口屏蔽优化

### 目标
在GUI界面上仅保留有效的B接口选项，移除其他无效接口选项。

### 具体修改点

#### 文件: `asr_gui.py`

**修改位置1 - ASR引擎选择下拉框**
- 当前代码中ComboBox包含多个选项：'B 接口', 'J 接口', 'K 接口', 'Whisper'
- **修改要求**：仅保留'B 接口'选项

**修改位置2 - ASRWorker类中的引擎判断逻辑**
- 当前代码中有多个elif分支处理不同引擎
- **修改要求**：
  - 移除J接口、K接口、Whisper的处理分支
  - 仅保留B接口的处理逻辑
  - 移除相关的import语句（JianYingASR, KuaiShouASR）

**修改位置3 - 相关的import语句**
- 移除无效的ASR引擎导入：
  ```python
  # 需要移除的导入
  from bk_asr.JianYingASR import JianYingASR
  from bk_asr.KuaiShouASR import KuaiShouASR
  ```

### 验证标准
- 界面启动后，ASR引擎选择下拉框只显示"B 接口"一个选项
- 程序运行正常，B接口功能完整可用
- 无其他接口相关的代码残留

---

## 2. 批量任务选择功能

### 目标
在任务列表中增加复选框，支持用户批量选择多个任务进行处理。

### 具体修改点

#### 文件: `asr_gui.py`

**修改位置1 - TableWidget列结构调整**
- 当前任务表格可能只有文件名、状态等列
- **新增要求**：在表格第一列添加复选框列

**修改位置2 - 表格数据模型更新**
- 修改表格初始化逻辑，支持复选框列
- 每行数据需要包含选中状态字段

**修改位置3 - 批量处理逻辑**
- 新增"批量处理选中任务"按钮
- 修改现有的处理逻辑，支持同时处理多个选中的文件
- 更新线程池管理逻辑，确保批量任务正确入队

**修改位置4 - UI布局调整**
- 在现有界面上添加批量操作相关控件：
  - "全选/取消全选"按钮
  - "批量处理选中任务"按钮
  - 可能需要调整现有按钮的位置和布局

**修改位置5 - 事件处理逻辑**
- 实现复选框状态变化的监听
- 实现全选/取消全选功能
- 实现批量处理的触发逻辑

### 技术实现建议

**表格复选框实现方案：**
```python
# 使用QTableWidgetItem配合Qt.ItemIsUserCheckable标志
item = QTableWidgetItem()
item.setFlags(Qt.ItemIsUserCheckable | Qt.ItemIsEnabled)
item.setCheckState(Qt.Unchecked)
```

**批量处理流程：**
1. 用户勾选需要处理的文件
2. 点击"批量处理选中任务"按钮
3. 系统遍历所有选中的文件，创建对应的ASRWorker
4. 将所有Worker提交到线程池处理
5. 更新对应行的处理状态

### 验证标准
- 任务表格第一列显示复选框
- 支持单个文件勾选和全选/取消全选操作
- 批量处理时，所有选中的文件都能正确进入处理队列
- 处理状态能够正确更新到对应的表格行
- 界面响应流畅，无卡顿现象

---

## 其他注意事项

### 代码清理
- 移除所有与无效接口相关的代码和注释
- 清理未使用的import语句
- 更新相关的错误处理逻辑

### 兼容性考虑
- 确保修改后的代码在Windows环境下正常运行
- 保持原有的ffmpeg音频转换功能不变
- 缓存机制和多线程处理逻辑保持不变

### 测试要点
1. 单文件处理功能正常
2. 批量选择和处理功能正常
3. 界面布局美观，无重叠或错位
4. 错误处理和异常情况下的用户体验
5. 大文件和多文件并发处理的稳定性

---
