# ASRTools v{APP_VERSION} 开发侧验证记录

- 目标平台：Windows 10+ x64
- 开发侧环境：Windows 11 x64
- Windows 10 x64：待用户对同一哈希产物手动复验
- FFmpeg：{FFMPEG_VERSION}
- FFmpeg SHA-256：`{FFMPEG_SHA256}`

本次仅执行构建、模型输入校验和压缩，不启动应用、不运行识别测试。
解压后的程序运行、批量识别和输出结果由交付方手动验证。
任何源码、依赖、模型或 FFmpeg 输入变化都需要重新构建并记录新产物哈希。
